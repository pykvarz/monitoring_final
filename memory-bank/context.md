# Memory Bank

## Project Brief
Десктопное Windows-приложение для мониторинга доступности сетевых узлов (хостов) в реальном времени.
Разрабатывается как **внутренний корпоративный инструмент** для системных администраторов и ИТ-специалистов.

### Ключевые сценарии
1. **Мониторинг**: периодический ICMP-пинг списка IP/хостов (IPv4 и IPv6, FQDN и однокомпонентные имена вроде `localhost`, `router`) с визуализацией статуса (Online / Waiting / Offline / Maintenance).
2. **Уведомления**: Desktop Toast-баннеры и системные уведомления Windows Tray при переходе узла в Offline.
3. **Управление хостами**: добавление, редактирование, удаление хостов и пользовательских групп с валидацией дубликатов IP/имен.
4. **Импорт/Экспорт**: массовый импорт и экспорт в Excel (с защитой от Formula Injection CWE-1236).
5. **История**: общий журнал смены статусов (Ctrl+H) и плавающее HUD-окно EventLogPanel («Поверх всех окон»).
6. **Helpdesk-интеграция**: ручное открытие/закрытие инцидентов из контекстного меню через Playwright.
7. **Темы оформления**: Dark (по умолчанию) и Tactical (NOC Terminal с моноширинным шрифтом).

### Целевая платформа
Windows 10/11. Поставка в виде автономного `.exe` (PyInstaller).

---

## System Patterns

### 1. Dependency Injection (DIContainer)
- Самописный легковесный DI-контейнер в `di_container.py`.
- Поддерживает: Singleton, Transient, Factory.
- `setup_container()` регистрирует `DatabaseManager`, `DataManager`, `HostRepository`, `StorageManager`, `PingService`, `NotificationService`.
- Главное окно и сервисы получают зависимости через `container.resolve(Interface)`.

### 2. Repository Pattern
- `HostRepository` (`core/host_repository.py`) — единая точка доступа к данным хостов поверх `DataManager`.
- Используется `MonitorThread`, `ContextMenuManager` и `HostManager`.

### 3. Thread Safety (QThread + сигналы Qt)
- `MonitorThread` изолирован в фоновом потоке.
- Прямая запись в БД из фонового потока запрещена — используется выделенное именованное SQLite-соединение (`monitor_thread_{id}`).
- Изменения статуса передаются в UI через сигналы Qt (`host_status_changed(id, status, offline_since)`).
- DataManager выполняет батч-обновление UI с подавлением дребезга (250 мс debounce через QTimer).

### 4. Domain Logic: Статус-машина
- `_calculate_status()` в `MonitorThread` — детерминированная статус-машина без I/O; при расчёте использует локальные кеши потока для непрерывности простоя.
- Переходы: `ONLINE → (нет ответа) → WAITING → (timeout) → OFFLINE`.
- Heartbeat throttling: сохранение `last_seen` в БД выполняется не чаще 1 раза в 60 секунд.
- Расчет времени простоя учитывает реальный `offline_since` с защитой от ложных таймеров.

### 5. Интерфейсы и абстракции
- `IStorageRepository`, `IPingService`, `INotificationService` в `interfaces.py` для полной изоляции и подмены моками в unit-тестах.

### 6. Журнал истории
- Таблица `status_history` в SQLite с версионированными миграциями (`schema_version`).
- Запись событий изменения статуса и автоматическая очистка по сроку хранения (`purge_old_history`).

### 7. Event Bus / Subscribers
- `subscribers/monitor_subscriber.py` реагирует на события DataManager (`host_added`, `host_deleted`, `host_info_updated`) и синхронизирует состояние `MonitorThread`.

### 8. Подтверждение Helpdesk
- Переход на новую карточку Naumen `#uuid:serviceCall$<числовой ID>` на том же сайте и пути подтверждает сохранение даже при видимых полях формы. Старый ID, смена только query, другой origin и авторизация не подтверждают отправку. Без подтверждения сообщаем о неопределённом результате и необходимости проверить заявку перед повтором.

---

## Tech Context

### Стек технологий
| Слой | Технология | Версия / примечание |
|------|-----------|---------------------|
| Язык | Python | 3.10+ (протестировано на 3.14) |
| UI Framework | PyQt5 | QWidget, QThread, QSqlDatabase, QSvg |
| БД | SQLite | Режим WAL, основное и именованное соединение потока мониторинга |
| Пинг | Win32 ICMP (iphlpapi.dll) / системный ping | Нативный in-process ICMP без прав администратора и без ping.exe (фоллбэк на системный ping) |
| Уведомления | PyQt5 Desktop Toast + QSystemTrayIcon | Нативные окна без активации фокуса |
| Экспорт/Импорт | openpyxl | 3.1.5 |
| Helpdesk | playwright | 1.62.0 (системный Edge/Chrome, опционально) |
| Сборка | PyInstaller | NetworkMonitor.spec / build_exe.py |
| Тесты | pytest | 304 теста + 59 subtests |

### Команды
- Установка зависимостей: `pip install -r requirements.txt`
- Установка dev-зависимостей: `pip install -r requirements-dev.txt`
- Запуск приложения: `python main.py`
- Тесты: `pytest`
- Запуск тестов с подробным выводом: `pytest -v`
- Сборка дистрибутива .exe: `python build_exe.py`
- Линтер и проверка типов не настроены (подтверждено пользователем).

### Файлы данных (runtime, исключены из git)
- `hosts.db` — рабочая база данных SQLite
- `config.json` — конфигурация приложения (`AppConfig`)
- `debug.log` — оперативный журнал работы

---

## Progress

### Статус: Активная разработка / Стабильный релиз

### Что реализовано
- [x] Высокопроизводительный параллельный мониторинг узлов (`ThreadPoolExecutor`) с фоллбэком на системный ping
- [x] Статус-машина (Online / Waiting / Offline / Maintenance) и учёт времени простоя
- [x] Хранилище SQLite WAL с автоматическими миграциями `schema_version`
- [x] Защита от потери данных и безопасная миграция `StorageManager` (JSON → SQLite)
- [x] Поддержка тем оформления: Dark, Tactical (NOC Terminal)
- [x] Адаптивная таблица хостов без пустых полей справа и с сохранением порядка/ширины колонок
- [x] Плавающее HUD-окно журнала событий (`FloatingEventLogWindow`) с режимом «Поверх всех окон»
- [x] Десктопные всплывающие Toast-уведомления с анимацией и тенью
- [x] Контекстные меню для узлов и пакетных операций с контурными иконками Lucide
- [x] Импорт/экспорт Excel с защитой от Formula Injection (CWE-1236)
- [x] Поддержка адресов IPv6 и однокомпонентных сетевых имен (`localhost`, `router`, `nas`)
- [x] Полнофункциональное управление группами узлов (создание, переименование с миграцией в БД, удаление с безопасным переносом в «Без группы»)
- [x] Полный набор тестов (pytest, 304 теста + 59 subtests: все пройдены)
- [x] Сборка .exe (PyInstaller + build_exe.py)

### Последние обновления
- **2026-09-23 — устранён ложный отказ после создания заявки Naumen:**
  - По скриншоту пользователя подтверждён маршрут сохранённой карточки `#uuid:serviceCall$<ID>`. Проверка распознаёт переход на новую карточку независимо от исчезновения полей; изменение query прежней карточки не считается сохранением.
  - Сообщения о неподтверждённом результате больше не утверждают, что форма не закрылась или заявка не создана.
  - 13 регрессионных сценариев; до исправления 5 падали. Полный набор: 304 passed, 59 subtests passed. Runtime E2E в Edge: заполнение формы → локальный HTTP save → новая hash-карточка с видимыми полями → ticket_created для «Установить» и «Снять»; HTTP 422 отклонён, браузерные ресурсы закрыты. Реальный корпоративный сервер не использовался.
- **2026-09-21 — устранение замечаний аудита и стабилизация жизненного цикла:**
  - `MonitorThread`: безопасное обновление размера пула воркеров (`_pending_max_workers` на границе цикла при активном мониторинге, немедленное обновление при остановленном потоке, корректный shutdown без невалидного `timeout`). Очистка кешей простоя при переходе в/из `MAINTENANCE`.
  - `DataManager`: `last_seen` обновляется только при действительном статусе `ONLINE` и `update_last_seen=True`, исключая подделку времени доступности при ручных операциях (`MAINTENANCE`).
  - `HelpdeskService`: верификация создания заявки проверяет смену URL карточки относительно исходного, ищет подтверждающие сообщения в контейнерах уведомлений (с поддержкой терминов «обращение», «инцидент», «зарегистрировано») и требует ID заявки при fallback на `body`.
  - `ExcelService`: все экспортируемые ячейки (включая статус) санитизируются ведущим апострофом при наличии символов формул (`=`, `+`, `-`, `@`) или управляющих символов (`\t`, `\r`), предотвращая Formula Injection (CWE-1236).
  - `StorageManager`: добавлена строгая валидация скалярных типов (`bool`, `int`, `str`) в `load_config()`.
  - `ExportImportManager`: убран неподдерживаемый формат `.xls` из фильтра диалога импорта.
  - `EventLogPanel`: подпись журнала согласована с реальным лимитом выборки (30 событий).
  - `README.md`: удалена устаревшая инструкция по `.env`, заменена описанием `config.json` и флага `--debug`.
  - Добавлены 5 регрессионных тестов в `tests/test_audit_regressions.py`. Полный набор: 291 passed, 59 subtests passed.
- **2026-09-21 — исправление трёх находок повторного аудита:**
  - Helpdesk подтверждает сохранение заявки по положительному сообщению в странице/iframe или URL карточки с числовым/UUID идентификатором; закрытие формы и переход на страницу ошибки больше не считаются успехом. Без известного подтверждения результат трактуется как неподтверждённый.
  - `StorageManager.load_hosts()` пропускает записи JSON неверного типа и безопасно обрабатывает неверный тип корня; `load_config()` отклоняет коллекции и их элементы неверного типа, подставляя значения по умолчанию для испорченных полей.
  - Добавлены 7 регрессионных тестов. Полный набор: 286 passed, 59 subtests passed. Runtime E2E: повреждённые JSON → Qt → ICMP localhost → SQLite → Excel; Edge DOM: подтверждение и страница ошибки.
- **2026-09-18 — устранение замечаний LOW и INFO аудита:**
  - `MonitorThread`: при прерывании цикла (`interrupt_cycle`) незавершённые задачи пула потоков (`futures`) отменяются через `f.cancel()`, освобождая воркеры пула от устаревших проверок.
  - `ContextMenuManager`: заголовок окна консольного пинга Windows теперь передаётся в команду консоли `cmd.exe /k "title {title} && ping -t {ip}"`, устранив неиспользуемую переменную `title`.
  - `MainWindow`: удалены черновые комментарии-вопросы из метода `_export_to_excel`.
  - Добавлен регрессионный тест `test_monitor_interrupt_cycle_cancels_pending_futures`. Тестовый набор расширен до 279 тестов + 59 subtests (все пройдены).
- **2026-09-18 — устранение 4 дефектов аудита (3 HIGH + 1 MEDIUM + 1 LOW):**
  - `Host.__post_init__` нормализует `None` в `address` (→ `""`) и `group` (→ `"Без группы"`) до проверки длины, предотвращая `TypeError` при загрузке записей с `NULL` из SQLite. В `data_manager.py` добавлен fallback `or "Без группы"` при чтении `grp`.
  - Миграция JSON → SQLite использует `os.replace` вместо `Path.rename`, устранив `FileExistsError [WinError 183]` на Windows при существующем `.json.bak`.
  - Сортировка групп в `main_window.py` нормализует `None` → `"Без группы"` при построении множества, предотвращая `TypeError` в `sorted()`.
  - Helpdesk: смена path на том же домене при сохранении заявки трактуется как успешный редирект на карточку, а не ошибка. Regex проверки auth-маркеров дополнен `\s` для корректного матча при trailing пробелах от `join`.
  - DDL схема SQLite: дефолт группы приведён с `'Default'` к `'Без группы'`.
  - Добавлены 4 регрессионных теста (TDD). Тестовый набор: 278 passed, 59 subtests passed.
- **2026-09-18 — исправления по итогам полного повторного аудита:**
  - Результаты ping применяются условно и больше не перезаписывают установленный оператором `MAINTENANCE`; прерывание цикла не теряет уже собранные уведомления, а `stop()` отменяет не начатые задания пула.
  - Миграция JSON → SQLite проверяет начало транзакции, каждую запись и commit; исходный JSON переименовывается только после успешной фиксации. Запись JSON выполняется атомарно через временный файл, а конфигурация неверного типа безопасно заменяется значениями по умолчанию.
  - `HostDialog` возвращает копию редактируемого узла и использует ограничения модели; Excel больше не обрезает допустимые hostname. Глобальный переключатель уведомлений отключает также desktop toast.
  - Helpdesk проверяет все обязательные поля, отклоняет редирект на авторизацию как подтверждение заявки и закрывает Playwright, даже если тот запустился после таймаута wrapper-задачи.
  - README приведён к фактическому поведению: Python 3.10+, две темы и ручной запуск Helpdesk из контекстного меню.
  - Добавлено 13 регрессионных тестов; полный набор: 274 passed, 59 subtests passed.
- **2026-09-18 — устранение регрессий повторного аудита:**
  - `MonitorThread` проверяет актуальный статус узла через существующий `HostRepository.get()` и именованное SQLite-соединение фонового потока; результаты ping снова записываются в БД без нарушения потоковой модели Qt SQL.
  - Проверка сохранения Helpdesk учитывает закрытие iframe, но подтверждает результат только при сохранении контекста страницы Helpdesk без редиректа на авторизацию.
  - 60-секундный таймаут Helpdesk начинается после захвата семафора; при дедлайне wrapper запрашивает остановку, а поздно созданный Playwright завершает себя до запуска браузера. Очередь удерживается до фактического завершения очистки.
  - Добавлены 7 регрессионных тестов с реальным циклом `MonitorThread`/SQLite и проверками жизненного цикла Helpdesk. Тестовый набор: 261 тест (261 passed); runtime E2E: Qt → ICMP localhost → SQLite и локальная iframe-форма в Edge — успешно.
- **2026-09-18 — устранение 10 дефектов и 1 риска безопасности (Astra Security & Quality Audit):**
  - **Безопасность IPv6 (Command Injection)**: В `models.py` (`validate_ip`) внедрена строгая валидация идентификатора зоны RFC 4007 (только `^[a-zA-Z0-9._-]+$`), исключив внедрение шелл-метасимволов оболочки. В `context_menu_manager.py` запуск непрерывного пинга на Windows переведён на прямой вызов `['cmd.exe', '/k', 'ping', '-t', ip]` с флагом `CREATE_NEW_CONSOLE` без уязвимой цепочки `cmd /c start`.
  - **Нативный Win32 ICMPv6 статус**: В `services.py` описаны точные структуры Win32 API `IPV6_ADDRESS_EX` (28 байт) и `ICMPV6_ECHO_REPLY` (`ipexport.h`). Считывание `Status` переведено на корректное смещение 28 байт вместо 32 (`RoundTripTime`), устранив ложные срабатывания недоступности IPv6 при RTT > 0.
  - **Сужение области доверия Windows SSO**: В `helpdesk_service.py` (`get_launch_args`) устранена широкая маска `*{domain}*`, заменённая на строгий домен `{domain},*.{domain}`. Неконтролируемое делегирование Kerberos `--auth-negotiate-delegate-allowlist` отключено по умолчанию.
  - **Синхронизация выделения Qt (Persistent Model Indexes)**: В `table_model.py` методы `sort()` и `update_hosts()` теперь выполняют маппинг `persistentIndexList()` через `changePersistentIndexList()`. В `host_manager.py` сбор идентификаторов хостов вынесен строго до открытия блокирующих модальных диалогов подтверждения.
  - **Валидация длины групп узлов**: В `dialogs.py` (`GroupManagerDialog`), `host_manager.py` (`change_group_selected`), `core/host_repository.py` и `data_manager.py` внедрена валидация длины названия группы (максимум 50 символов) с предупреждением пользователя, предотвратив рассинхрон с валидацией `Host` и сбои при загрузке.
  - **Гонка статуса MAINTENANCE**: В `monitor_thread.py` метод `_calculate_status` теперь безусловно сохраняет статус `MAINTENANCE` независимо от результата пинга (`ONLINE`/`OFFLINE`). В цикле обработки результатов добавлена проверка актуального состояния в БД, отбрасывающая устаревшие пинги при ручной смене режима.
  - **Синхронизация настроек и пула потоков**: В `monitor_thread.py` метод `update_config` сравнивает `max_workers` с действующим размером пула `_executor`, гарантируя применение изменений даже при мутации объекта конфигурации. В `main_window.py` добавлено сохранение флага `helpdesk_headless`.
  - **Робастность Helpdesk и очереди Playwright**: В `helpdesk_service.py` разделены селекторы CSS и XPath в `_find_form_context`, исключив ошибку DOMException в Chromium/Edge. Проверка закрытия формы переведена на активный контекст `form_ctx`. Таймаут выполнения задачи перенесён строго после захвата семафора, а `_log_future_error` гарантированно отправляет сигнал `ticket_failed` при любых сбоях и таймаутах очереди.
  - Добавлены тестовые наборы: `tests/test_phase1_security_network.py` (8 тестов), `tests/test_phase2_state_data_integrity.py` (6 тестов), `tests/test_phase3_helpdesk_robustness.py` (3 теста). Тестовый набор расширен до 254 тестов (254 passed). Успешно пройден сквозной runtime E2E тест.
- **2026-09-17 — точные селекторы полей Naumen SD по подтвержденным gwt-debug ID:**
  - В `helpdesk_service.py` внедрены прямые селекторы по подтвержденным GWT Debug ID инсталляции: `gwt-debug-agreementServiceProperty-value` («Соглашение/Услуга»), `gwt-debug-servCategory-value` («Категория услуги»), `gwt-debug-subCategory-value` («Подкатегория»), `gwt-debug-location-value` («Местонахождение»), `gwt-debug-shortDescr-value` («Тема»), `gwt-debug-description-value` («Описание»), `gwt-debug-apply` (кнопка «Сохранить»).
  - Реализовано открытие кастомных выпадающих списков `formSelect` кликом по внешнему GWT-триггеру, регистронезависимый поиск элементов списка, паузы между каскадными AJAX-обновлениями списков, клавиатурный ввод через `press_sequentially` для GWT SuggestBox с фиксацией `ArrowDown` + `Enter`.
  - В `fill_field` добавлена фиксация фокуса (`press("Tab")`) для надёжного коммита значений в модель GWT.
  - Сохранены универсальные фоллбэки по текстовым меткам и поиск во фреймах iframes.
- **2026-09-17 — рефакторинг helpdesk_service.py по результатам аудита (MED-1..4, LOW-1/3/4, INFO-1):**
  - Извлечены вложенные `async def` (`select_dropdown`, `fill_field`, описание) в отдельные `@classmethod` уровня класса (`_select_dropdown`, `_fill_field`, `_fill_description`, `_find_form_context`), сократив монолитный `_process_ticket_task_async` с ~310 до ~90 строк.
  - Добавлен `_xpath_escape()` для безопасного экранирования строк в XPath-выражениях (защита от поломки при одинарных/двойных кавычках в метках).
  - Пост-сохранительная верификация: после клика «Сохранить» ожидается исчезновение маркеров формы (`#gwt-debug-location-value`, `#gwt-debug-shortDescr-value`) в течение 5 сек; если форма остаётся видимой — `ticket_failed` со скриншотом вместо ложного `ticket_created`.
  - Строгая валидация обязательных полей: `and` → `or` (блокировка при незаполнении _любого_ из полей «Местонахождение»/«Тема», а не только обоих одновременно).
  - Семафор `asyncio.Semaphore(1)` инициализируется детерминировано в `_start_loop()` после создания event loop.
  - Переменная `e` в `except PlaywrightTimeoutError`/`asyncio.TimeoutError` теперь включается в лог-сообщения и `ticket_failed`.
  - Затенение переменной `ctx` устранено: внутренние циклы переименованы в `search_ctx`.
  - Добавлено 10 новых тестов: `TestHelpdeskXPathEscape` (5 тестов) + `TestHelpdeskFormMethodsMock` (5 async-тестов с mock Playwright). Тестовый набор: 238 тестов (238 passed).
- **2026-09-17 — робастный поиск полей GWT/Iframe и защита от ложного сохранения в Helpdesk:**

  - В `helpdesk_service.py` реализован динамический поиск контекста формы через сканирование `page` и всех `page.frames` с адаптивным ожиданием до 25 секунд (поддержка модальных фреймов Naumen SD).
  - Универсальные локаторы полей `fill_field` и `select_dropdown` переведены на поиск по любому тегу (`label`, `div`, `span`, `td`, `th`), устранив отказ при GWT-разметке (`div.gwt-Label`, `td.gwt-HTML`). Добавлен поиск связанных `input` в строках таблиц `ancestor::tr[1]//input` и родительских блоках.
  - Добавлена строгая предсохранительная валидация (Anti-False-Positive): при отсутствии заполнения полей («Местонахождение», «Тема») сохранение блокируется, создается скриншот ошибки и генерируется сигнал сбоя `ticket_failed`.
  - Из `SAVE_BUTTON_SELECTOR` исключен общий селектор `input[type='submit']`, исключив ошибочный клик по посторонним кнопкам.
- **2026-09-17 — исправление доменной авторизации Windows SSO (NTLM/Kerberos) в Helpdesk:**
  - В `helpdesk_service.py` реализован метод `get_launch_args(url: str)`: устранены экранированные двойные кавычки из флага `--auth-server-allowlist`, которые блокировали сопоставление домена в движке Chromium и вызывали всплывающее модальное окно «Вход в систему» с запросом сетевого логина и пароля.
  - Добавлены параметры делегирования `--auth-negotiate-delegate-allowlist=*{domain}*` и явное указание схем `--auth-schemes=basic,digest,ntlm,negotiate`.
  - Системный Microsoft Edge теперь автоматически и прозрачно подхватывает учетную запись текущего пользователя Windows (SSO) при создании заявок без запроса логина/пароля.
  - В `tests/test_helpdesk_service.py` добавлен класс тестов `TestHelpdeskLaunchArgs` (3 теста). Тестовый набор расширен до 228 тестов (228 passed).
- **2026-09-16 — расстояние между таблицей и журналом событий (сплиттер):**
  - В `main_window.py` для `_content_splitter` установлен `setHandleWidth(10)` и `setChildrenCollapsible(False)`, устранив плотное склеивание границ таблицы и журнала событий и сформировав гармоничный 10px отступ в стиле дизайн-системы.
  - В `constants.py` (`get_main_style`) добавлена стилизация `QSplitter::handle` для тем Dark и Tactical: прозрачный фон с аккуратной 2px акцентной линией-индикатором в центре при наведении (`margin: 0px 4px`).
- **2026-09-16 — актуализация README.md под текущее состояние проекта:**
  - Обновлены ключевые разделы: нативный Win32 ICMP движок (Win32 API `iphlpapi.dll`, нулевая нагрузка на EDR, полное удаление ping3), менеджер групп хостов (редактирование, удаление, счетчики узлов, защита системной группы «Без группы»), интеграция с Helpdesk Naumen SD (ручное создание/закрытие заявок, форматирование номера 0000, поддержка headless/видимого режима, безопасные скриншоты ошибок).
  - Актуализирована таблица горячих клавиш (Ctrl+Space, Ctrl+H, Ctrl+G, Ctrl+N, Ctrl+P, Ctrl+I, Ctrl+E, F5, Delete, Esc), контекстные меню и массовые операции.
  - Добавлена визуальная карта архитектуры и файлов проекта.
  - Скорректировано количество тестов: 225 тестов (225 passed).
- **2026-09-16 — устранение замечаний аудита безопасности и оптимизация Helpdesk:**
  - Оптимизация скриншотов Helpdesk (`helpdesk_service.py`): удалён избыточный снимок `helpdesk_preview.png` при успешном создании заявки (ускорена работа и предотвращено засорение диска); скриншоты ошибок `helpdesk_error.png` перенесены в системный временный каталог (`tempfile.gettempdir()`) с уникальным именем хоста, устранив риск `PermissionError` при запуске на доменных ПК с произвольным CWD.
  - Защита от параллельного запуска браузеров (`helpdesk_service.py`): добавлен `asyncio.Semaphore(1)` для очереди сессий Playwright при быстрых повторных кликах оператора.
  - Устранение Command Injection (`context_menu_manager.py`): в вызов Windows `cmd /c start` добавлена санитизация `label` от кавычек и метасимволов оболочки `cmd.exe` (`"`, `&`, `|`, `<`, `>`, `^`, `%`) с нормализацией пробелов.
  - Валидация статуса ICMPv6 (`services.py`): в Win32 API `Icmp6SendEcho2` добавлена проверка поля `Status == 0` (`IP_SUCCESS`) в структуре `ICMPV6_ECHO_REPLY` в дополнение к `ret > 0`, исключив ложноположительный статус Online при получении ICMPv6 пакетов ошибок.
  - Качество кода (`dialogs.py`, `main_window.py`): добавлена санитизация `.strip()` для группы в `HostDialog` и удалён неиспользуемый импорт `subprocess` в `main_window.py`.
  - Добавлены тесты `test_error_screenshot_path_in_temp` и `test_ping_cmd_windows_sanitizes_dangerous_label`. Тестовый набор расширен до 225 тестов (225 passed).
- **2026-09-16 — исправление раскрытия списков QComboBox (строго вниз) и нормализация кнопок журнала событий:**
  - Направление выпадающих списков (`theme_manager.py`, `main.py`): переопределен хинт `SH_ComboBox_Popup = 0` через `AppStyle(QProxyStyle)` поверх Fusion. Выпадающие списки больше не центрируются вверх поверх родительского виджета с выталкиванием пунктов вверх, а раскрываются строго вниз.
  - Геометрия кнопок журнала (`constants.py`, `history_views.py`): ограничение `min-width: 65px` снято с общего селектора `QPushButton` и перенесено строго на `QDialogButtonBox` / `QMessageBox`. Для иконочных кнопок `_btn_clear`, `_btn_refresh`, `_btn_dock`, `_btn_pin` жестко заданы границы `min-width: 0px; max-width: 26px; min-height: 26px; max-height: 26px;`, устранив растяжение кнопок. Ширина комбобокса статусов расширена до `min-width: 110px`, устранив обрезку текста («Тех.обслуживание»).
- **2026-09-16 — устранение белого фона кнопок и списков в диалогах (стилизация QSS):**
  - В `constants.py` расширены стили `get_main_style` для тем Dark и Tactical: добавлены глобальные правила для `QPushButton` (состояния default, hover, pressed, disabled) и `QListWidget, QListView` (фон, границы, цвет текста и выделения).
  - В `dialogs.py` устранена потеря наследования стилей: удалены жесткие локальные вызовы `setStyleSheet("QGroupBox { font-weight: bold; }")` у групп параметров в `SettingsDialog`, которые ломали стилизацию вложенных кнопок (`manage_groups_btn`).
  - В `GroupManagerDialog` локализована кнопка закрытия диалога («Закрыть» вместо «Close»).
- **2026-09-16 — нативный Win32 ICMP движок и полное удаление зависимости ping3:**
  - В `services.py` реализован сетевой движок на базе официального Win32 API `iphlpapi.dll`: `IcmpSendEcho` (для IPv4) и `Icmp6SendEcho2` (для IPv6) через стандартный модуль `ctypes`.
  - Работает на полной скорости прямо в процессе приложения без прав администратора и не порождает дочерних процессов `ping.exe`. Это полностью решает проблему микрофризов и нагрузки на CPU от доменных антивирусов/EDR (Kaspersky, Defender ATP и др.), перехватывающих запуск процессов.
  - Внешняя зависимость `ping3` полностью удалена из проекта: очищены `requirements.txt`, `NetworkMonitor.spec`, `build_exe.py`, `main.py`, `README.md`, `SECURITY.md`.
  - Модуль тестов `tests/test_ping_service.py` переписан под Win32 ICMP и системный фоллбэк с реальными нативными тестами loopback (`127.0.0.1`, `::1`). Тестовый набор расширен до 223 тестов (223 passed).
- **2026-09-16 — оптимизация производительности (UI, фильтрация, журнал событий и подавление I/O спама):**
  - Оптимизация журнала событий (`history_views.py`): лимит выборки событий в `EventLogPanel.refresh()` снижен с 200 до 30 карточек (устранена генерация сотен тяжелых составных QWidget'ов с версткой в основном UI-потоке).
  - Отвязка от тиков мониторинга (`main_window.py`): вызов `self._event_log_panel.refresh()` убран из `_on_hosts_updated` (где он вызывался до 4 раз в секунду при каждом 250мс батч-обновлении) и перенесен в `_on_scan_finished` (вызывается строго 1 раз за полный цикл обхода сети).
  - Быстрая фильтрация таблицы (`filter_manager.py`): добавлен Early Exit (при отсутствии активных фильтров и скрытых строк `apply_filters()` завершается мгновенно `O(1)` без обхода таблицы); вызов `setRowHidden` происходит только при реальном изменении видимости строки, исключая лишнюю инвалидацию layout и фризы таблицы.
  - Подавление дискового I/O спама (`services.py`): при отсутствии прав администратора (доменная машина) и невозможности открыть сырой сокет `ping3` логирование переведено с `warning` на `debug`, предотвращая непрерывную запись сотен строк в `debug.log` каждую минуту.
  - Добавлен тест `test_apply_filters_early_exit_no_hidden_rows` в `tests/test_filter_manager.py`. Тестовый набор расширен до 221 теста (221 passed).
- **2026-09-16 — переключатель фонового (Headless) режима Helpdesk в Настройках:**
  - В `models.py` (`AppConfig`) добавлено поле `helpdesk_headless: bool = False` с сохранением в `config.json`.
  - В `SettingsDialog` (`dialogs.py`) в секцию «Интеграция с Helpdesk» добавлен чекбокс «Скрытый фоновый режим браузера (Headless)» с подсказкой и двусторонней синхронизацией.
  - В `helpdesk_service.py` параметр `headless` передается из конфигурации в асинхронные задачи `process_offline` / `process_recovered` и браузерный движок Playwright; в видимом режиме сохранена комфортная 2-секундная задержка для визуального контроля перед закрытием, в headless-режиме процесс завершается без лишних задержек.
  - Написаны тесты для модели и диалога в `tests/test_models.py` и `tests/test_helpdesk_service.py`. Тестовый набор расширен до 220 тестов (220 passed).
- **2026-09-16 — стабилизация Helpdesk-интеграции (GWT Naumen SD, видимый режим, авто-скриншоты):**
  - Устранена ошибка «Кнопка Сохранить не найдена»: в `helpdesk_service.py` обновлен селектор с точной привязкой к нативному GWT debug-идентификатору (`#gwt-debug-apply`, `#gwt-debug-buttons .g-button`, `div.g-button:has-text('Сохранить')`).
  - Устранена гонка рендеринга: мгновенный `.count() > 0` заменен на явное ожидание появления формы и кнопки в DOM (`wait_for(state="visible", timeout=15000)`).
  - Включен видимый режим браузера (`headless=False`), чтобы администратор визуально контролировал открытие формы, автозаполнение полей и клик сохранения в реальном времени.
  - Добавлена нормализация URL (`HelpdeskService.normalize_url`): авто-добавление протокола `https://` при копировании ссылок без схемы из адресной строки браузера.
  - Добавлено сохранение контрольного скриншота формы перед отправкой (`helpdesk_preview.png`) и скриншота экрана при сбое (`helpdesk_error.png`).
  - Тестовый набор расширен до 217 тестов (217 passed, `tests/test_helpdesk_service.py`).
- **2026-09-15 — полнофункциональное управление группами узлов (создание, редактирование, удаление):**
  - Разработан диалог `GroupManagerDialog` в `dialogs.py`: отображение списка групп с подсчетом количества хостов в каждой группе, кнопки «Добавить...», «Переименовать...», «Удалить» и «Закрыть».
  - Защита системной дефолтной группы «Без группы»: блокировка кнопок переименования и удаления в UI и валидация на уровне логики с информативным `QMessageBox.warning`.
  - В `data_manager.py` и `core/host_repository.py` добавлены методы:
    - `get_groups_with_counts() -> List[Tuple[str, int]]` — получение групп с реальным числом хостов.
    - `rename_group(old_name, new_name) -> int` — пакетное переименование группы у всех узлов в БД SQLite с оповещением подписчиков через `_trigger_update()`.
    - `delete_group(group_name, fallback_group="Без группы") -> int` — удаление группы с автоматическим переносом всех хостов в дефолтную группу.
  - В `SettingsDialog` добавлена секция «Группы узлов» с кнопкой «Управление группами...».
  - На панели быстрых действий `main_window.py` обновлена кнопка и шорткат `Ctrl+G` («Управление группами (Ctrl+G)»).
  - Реализована синхронизация `config.custom_groups` с сохранением в `config.json`.
  - Написан тестовый модуль `tests/test_group_manager.py` (7 тестов). Тестовый набор расширен до 213 тестов (213 passed). Проведен успешный сквозной runtime E2E тест.
- **2026-09-15 — форматирование номера банкомата с префиксом 0000 в Helpdesk:**
  - В `helpdesk_service.py` добавлен метод `HelpdeskService.format_atm_number(host_name)`: к имени/номеру банкомата добавляется префикс `0000` (например, `1234` → `00001234`) с защитой от повторного дублирования префикса и очисткой пробелов.
  - Форматированный номер подставляется во все целевые текстовые поля веб-формы Helpdesk: «Местонахождение», «Тема» и «Описание» (`1. Лог. № банкомата: 0000...`).
  - Добавлены модульные тесты `tests/test_helpdesk_service.py`, тестовый набор расширен до 206 тестов (206 passed).
- **2026-09-15 — устранение замечаний аудита безопасности и архитектуры (AUDIT-1..5):**
  - AUDIT-1 (`.gitignore`): Добавлено игнорирование `.env` и `.env*.local` для защиты от утечки секретов и токенов в репозиторий.
  - AUDIT-2 (`storage.py`, `di_container.py`, `main.py`): Параметр `base_dir` в `StorageManager`, привязка `hosts.db`, `hosts.json`, `config.json` и `debug.log` к базовой директории приложения/исполняемого файла для защиты от сброса путей при запуске из ярлыка Windows.
  - AUDIT-3 (`excel_service.py`): Усилена защита от Formula Injection (CWE-1236) — добавлена санитизация строк с начальными пробельными символами перед формульными триггерами (`=`, `+`, `-`, `@`, `\t`, `\r`).
  - AUDIT-4 (`context_menu_manager.py`): В вызов Windows `cmd /c start` для непрерывного ping добавлен явный информативный заголовок окна для предотвращения коллизий парсинга аргументов в cmd.exe.
  - AUDIT-5 (`.env.example`): Очищены устаревшие переменные веб-шаблона (`PORT=3000`, `NODE_ENV`), добавлены примеры десктопной конфигурации и Helpdesk.
  - Тестовый набор расширен до 202 тестов (202 passed).
- **2026-09-15 — сохранение готовых и пользовательских причин Helpdesk:**
  - Устранена потеря причин заявок: в `main_window.py` добавлено сохранение `helpdesk_reasons` и `helpdesk_reasons_recovered` из диалога настроек.
  - В `context_menu_manager.py` добавлено автосохранение новой введенной пользователем причины («своей версии») прямо в конфигурацию и файл `config.json`.
  - В `models.py` и `dialogs.py` разделены готовые причины для открытия и закрытия заявок. Тестовый набор расширен до 198 тестов (198 passed).
- **2026-09-15 — фильтрация заявок Helpdesk по группе узла (АТМ):**
  - В `context_menu_manager.py` добавлен метод `is_atm_group`: пункты открытия и закрытия заявок Helpdesk в контекстном меню отображаются и вызываются исключительно для узлов из группы «АТМ» (поддерживаются «АТМ», «ATM», «Банкоматы»). Тестовый набор расширен до 197 тестов (197 passed).
- **2026-09-15 — оптимизация Helpdesk-интеграции (Playwright):**
  - В `helpdesk_service.py` реализован запуск через системный Microsoft Edge с маскировкой автоматизации (`--disable-blink-features=AutomationControlled`) и каскадным фоллбэком на Chrome / Chromium без необходимости отдельного скачивания `playwright install`.
- **2026-09-14 — актуализация документации:**
  - Полностью обновлены [README.md](file:///c:/Users/User/Desktop/Шаблон%20—%20копия/README.md) и [SECURITY.md](file:///c:/Users/User/Desktop/Шаблон%20—%20копия/SECURITY.md) под актуальную архитектуру, токены тем, HUD, Toast-уведомления и защитные механизмы (CWE-1236, защита от Command Injection, изоляция БД).
- **2026-09-14 — устранение дефектов Medium и Low (MED-1..3, LOW-1..2):**
  - MED-1: В `models.py` добавлена поддержка IPv6 через `ipaddress.ip_address` и валидация однокомпонентных сетевых имен (`localhost`, `router`, `dc01`).
  - MED-2: В `theme_manager.py` удален мертвый код (`_update_toolbar_buttons`, `_update_filter_buttons`) с необъявленной функцией `get_svg_theme`.
  - MED-3: Из `requirements.txt` удалена неиспользуемая зависимость `plyer==2.1.0`.
  - LOW-1: В `filter_manager.py` удалены устаревшие рудименты `.replace("📁 ", "")`.
  - LOW-2: В `services.py` исправлен расчет таймаута для Linux системного ping (`-W` в секундах, а не миллисекундах).
  - Тестовый набор расширен до 195 тестов (195 passed).
- **2026-09-14 — устранение дефектов High (HIGH-1..3):**
  - HIGH-1: Сохранение настроек HUD и сплиттера в `SettingsDialog.get_config` (`dialogs.py`).
  - HIGH-2: Устранение `NameError: SVG_MAINTENANCE` в `ContextMenuManager.show_bulk_menu` (`context_menu_manager.py`).
  - HIGH-3: Сохранение поля `address` и дескриптора БД в `StorageManager.migrate_to_db` (`storage.py`).
- **2026-09-14 — устранение критических дефектов (CRIT-1..3):**
  - CRIT-1: Защита сортировки таблицы от `AttributeError` при `NULL` в SQLite (`table_model.py`).
  - CRIT-2: Защита от `KeyError: 'UNKNOWN'` при экспорте в Excel и подсказках (`excel_service.py`, `table_model.py`).
  - CRIT-3: Корректный жизненный цикл закрытия плавающего HUD окна (`main_window.py`).

---

## Design System

### 1. Токены
#### Статусы узлов (Semantic Status Colors)
- **ONLINE**: `#10b981` (Изумрудно-зеленый) — узел доступен.
- **WAITING**: `#f59e0b` (Теплый янтарный) — временный сбой, ожидание подтверждения.
- **OFFLINE**: `#ef4444` (Рубиново-красный) — узел недоступен, авария.
- **MAINTENANCE**: `#8b5cf6` (Фиолетовый) — регламентные работы / тех. обслуживание.
- **TOTAL**: `#3b82f6` (Синий) — общее количество узлов.

#### Темы оформления
- **Dark Theme (по умолчанию)**: фон `#151820`, поверхность карточек `#181c26`, границы `#282e3d`, текст `#f1f5f9` / `#94a3b8`, зебра таблицы `#1e222e`.
- **Tactical Theme (NOC Terminal)**: фон `#090A0F`, карточки `#0D1117`, границы `#1F232D`, моноширинный шрифт `Consolas`, неоновые акценты (`#39FF14`, `#00F0FF`).

### 2. Компоненты UI
- **Иконки**: строго плоские векторные контурные SVG-иконки Lucide (`stroke-width="2"`). Системные цветные эмодзи в интерфейсе запрещены.
- **Карточки метрик (Dashboard Cards)**: полупрозрачная подложка, нейтральный бордер, радиус 8-10px, акцентная иконка и крупный показатель.
- **Таблица хостов**: адаптивное растяжение смысловых колонок, мягкая цветовая подсветка строк без тяжелой заливки.
- **HUD-панель (EventLogPanel)**: карточки с левым цветным акцентом статуса, поддержка открепления в плавающее окно с Always on Top.
- **Панель инструментов (Action Toolbar)**: логическая группировка кнопок с тонкими разделителями.

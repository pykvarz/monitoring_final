# Progress — Network Monitor

## Статус: Активная разработка

## Что реализовано (известно по коду)

### Ядро
- [x] Мониторинг узлов (ICMP ping, параллельный — ThreadPoolExecutor)
- [x] Фоллбэк на системный ping.exe при отсутствии прав администратора
- [x] Статус-машина: Online / Waiting / Offline / Maintenance
- [x] Heartbeat throttling (запись last_seen раз в 60 сек)
- [x] Корректный учет времени простоя: кеширование recovery_times и offline_since для сброса ложных таймеров
- [x] Thread-safe обновление UI через сигналы Qt
- [x] Батч-обновления UI (250 мс debounce)

### Хранение данных
- [x] SQLite (PyQt5.QtSql) с WAL-режимом и индексами
- [x] Версионированные миграции схемы БД через таблицу schema_version (database.py)
- [x] История смены статусов (status_history)
- [x] Очистка истории по сроку хранения
- [x] Миграция из JSON → SQLite (legacy)
- [x] config.json для AppConfig

### UI
- [x] Главное окно с таблицей хостов
- [x] Редизайн UI: удалены эмодзи-костыли, внедрена единая дизайн-система токенов (memory-bank/designSystem.md)
- [x] Поддержка тем: Dark, Light и Tactical (NOC Terminal с моноширинным шрифтом)
- [x] Адаптивное растяжение колонок таблицы (устранена черная пустота справа)
- [x] Деликатная цветовая подсветка строк таблицы (сохранение читаемости и структуры zebra-striping)
- [x] Правая панель EventLogPanel на базе QFrame с рамкой, темными SVG-кнопками и Empty State
- [x] Логическая группировка панели действий (сепараторы между блоками кнопок)
- [x] Кнопка паузы мониторинга (с индикатором в статус баре и горячей клавишей Ctrl+Space)
- [x] Современная система всплывающих toast-уведомлений (анимированные карточки в правом нижнем углу)
- [x] Обновлённый дизайн карточек ленты событий (EventCardWidget с цветными акцентами)
- [x] Дашборд-панель (статистика)
- [x] SVG-иконки статусов (HiDPI)
- [x] Фильтрация по группам
- [x] Контекстное меню (правая кнопка мыши)
- [x] Диалоги добавления/редактирования хоста
- [x] Панель истории узла

### Сервисы
- [x] Уведомления (PyQt5 QSystemTrayIcon + встроенный ToastManager для UI)
- [x] Импорт/Экспорт Excel (openpyxl)
- [x] Helpdesk-интеграция (playwright, опционально)

### Инфраструктура
- [x] DI Container (самописный)
- [x] Repository Pattern
- [x] Интерфейсы-абстракции для тестирования
- [x] Очистка мертвого кода (удалены logger_setup.py, menu_builder.py)
- [x] Набор тестов (pytest, 153 теста: 146 passed, 7 skipped)
- [x] Сборка .exe (PyInstaller + build_exe.py)

## Открытые вопросы / возможные точки роста
- `StorageManager` (JSON) частично дублирует функционал `DataManager` (SQLite)
- `playwright` — тяжёлая зависимость для helpdesk, возможно заменить на `requests`
- Неиспользуемая библиотека `plyer` в requirements.txt (заменена на QSystemTrayIcon)

## Последнее обновление memory-bank
2026-09-10 — Безопасность и стандарты (Приоритет 2 аудита):
- Защита от Formula Injection (CWE-1236) при экспорте в Excel (`excel_service.py`): экранирование ведущим апострофом строковых ячеек, начинающихся с `=`, `+`, `-`, `@`, `\t`, `\r`.
- Проверка дубликатов IP/хостнеймов при ручном добавлении и редактировании узлов (`host_manager.py`, `core/host_repository.py`, `data_manager.py`).
- Безопасный вызов внешних утилит в контекстном меню (`context_menu_manager.py`): предварительная валидация адреса через `validate_ip_or_hostname` и передача безопасного списка аргументов на Linux без склеивания shell-строк.
- Строгое соблюдение стандартов освобождения ресурсов SQLite (`data_manager.py`, `database.py`): явная передача дескриптора БД `QSqlDatabase` во все экземпляры `QSqlQuery` и гарантированный вызов `query.finish()` во всех ветках выполнения.
- Тестовый набор расширен до 164 тестов (158 passed, 6 skipped).

2026-09-10 — Устранение критических сбоев ядра (Приоритет 1 аудита):
- Устранён критический краш `TypeError` при сортировке таблицы по колонке «IP адрес» при наличии хостов с доменными именами (`table_model.py`).
- Защищён доступ к `ThreadPoolExecutor` в `MonitorThread` через `threading.Lock` при динамическом обновлении конфигурации (`update_config`), устранена утечка именованного SQLite-подключения потока (`monitor_thread.py`).
- Добавлен таймаут (`timeout + 2.0`) и перехват `subprocess.TimeoutExpired` для `subprocess.run` в `PingService._system_ping` для предотвращения зависания воркеров (`services.py`).
- Исправлена валидация IP/доменных имен в `models.py` (блокировка некорректных 4-октетных чисел и числовых TLD), расконсервирован тест `test_invalid_ip`.
- Тестовый набор расширен до 157 тестов (151 passed, 6 skipped).

2026-09-10 — актуализация по результатам Drift Detection:
- Зафиксирована поддержка темы Tactical (NOC Terminal) в UI и designSystem.
- Зафиксирована система миграций schema_version в SQLite (закрыт техдолг).
- Уточнены тесты (153 теста) и удаление устаревших модулей (menu_builder.py, logger_setup.py).
- Отражен фоллбэк ICMP ping на системный ping.exe без прав администратора и перевод уведомлений на QSystemTrayIcon.

2026-09-07 — полная адаптация тёмной темы:
- Стилизованы всплывающие окна главного меню и контекстных меню (QMenu) с тёмным фоном, контрастным текстом и иконками.
- Настроены выпадающие списки (QComboBox QAbstractItemView), устранены дефекты «белый текст на белом фоне».
- Интегрирован тёмный системный заголовок Windows 10/11 через DWMWA_USE_IMMERSIVE_DARK_MODE.
- Все диалоговые окна (HostDialog, SettingsDialog, HistoryDialog, HostHistoryDialog) и поля ввода переведены на тёмные токены.
- Установлена тёмная системная палитра QApplication для предотвращения появления светлых нативных виджетов.

